# Optional: override Vite `base` for production assets and the router.
# Omit for root hosting (https://example.com/ or https://user.github.io/).
# GitHub Actions sets this automatically from the repo name for project pages.
# For a manual build to a project site: VITE_BASE=my-repo-name
